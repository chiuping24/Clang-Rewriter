#include "../Header.h"

void do_math(int *a) {
	auto char buf[26];
	*a += 5;
}

/*double fabs(double x) {
	double a;
	if (x > 0|| x==0) { a = x; }
	if (x < 0) { a = -1 * x; }
	return a;
}*/

